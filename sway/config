### Variables
set $mod Mod4
set $left h
set $down j
set $up k
set $right l
set $term foot
set $menu fuzzel
set $lock_cmd swaylock

### Gruvbox Colors
set $bg      #1d2021
set $gray    #928374
set $dark_gray #262626
set $green   #689d6a
set $red     #fb4934
set $yellow  #d79921
set $text    #ebdbb2

### Output configuration
output * bg "/home/andrew/Images/Wallpapers/coding-2.png" fill


### Key bindings
### Basics
bindsym $mod+Return exec $term 
bindsym $mod+Shift+q kill
bindsym $mod+d exec $menu
floating_modifier $mod normal 

### Reload Sway and Waybar
bindsym $mod+Shift+c reload; 

### Swaylock (Standard)
bindsym $mod+escape exec $lock_cmd

### Power menu keybinding
bindsym $mod+Shift+e exec swaylock

### Firefox
bindsym $mod+Shift+f exec firefox

### Navigation (Vim-style)
bindsym $mod+$left focus left 
bindsym $mod+$down focus down 
bindsym $mod+$up focus up 
bindsym $mod+$right focus right 

### Split windows directions
# Binds to split horizontally ($mod + b)
bindsym $mod+b splith

# Binds to split vertically ($mod + v)
bindsym $mod+v splitv

### Moving windows
bindsym $mod+Shift+$left move left 
bindsym $mod+Shift+$down move down 
bindsym $mod+Shift+$up move up 
bindsym $mod+Shift+$right move right 

### The same but with arrow keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

### Workspace
bindsym $mod+1 workspace number 1 
bindsym $mod+2 workspace number 2 
bindsym $mod+3 workspace number 3 
bindsym $mod+4 workspace number 4 
bindsym $mod+5 workspace number 5 
bindsym $mod+Shift+1 move container to workspace number 1 
bindsym $mod+Shift+2 move container to workspace number 2 
bindsym $mod+Shift+3 move container to workspace number 3 
bindsym $mod+Shift+4 move container to workspace number 4 
bindsym $mod+Shift+5 move container to workspace number 5 

### Layout & Multimedia
bindsym $mod+f fullscreen 
bindsym $mod+Shift+space floating toggle
bindsym --locked XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle 
bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5% 
bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5% 

# Special keys to adjust brightness via brightnessctl
bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+


### Spacing & Window Rules
default_border pixel 2
smart_gaps off

gaps inner 10
gaps outer 0
gaps top -4

### Appearance (Colors)

### class               border  backgr. text    indicator child_border
client.focused          $green  $green  $bg     $green    $green 
client.unfocused        $gray   $bg     $gray   $bg       $gray 
client.focused_inactive $gray   $bg     $gray   $bg       $gray 
client.urgent           $red    $red    $text   $red      $red 

### swaybar
# Use instead of waybar for more minimal setups
#bar {
#    position top
#    font pango:JetBrainsMono Nerd Font 11
    
    # Keeps everything left-aligned next to workspaces
#    pango_markup enabled

#    status_command while ~/.config/sway/swaybar/status.sh; do sleep 2; done

#    colors {
#        background #1d2021
#        statusline #ebdbb2
#        separator  #928374

#        focused_workspace  #1d2021 #1d2021 #689d6a
#        active_workspace   #1d2021 #1d2021 #928374
#        inactive_workspace #1d2021 #1d2021 #928374
#        urgent_workspace   #1d2021 #1d2021 #fb4934
#    }
#}

### Autostart
exec_always "pkill waybar; waybar"
exec swayidle -w timeout 600 'swaylock' before-sleep 'swaylock' 

include /etc/sway/config.d/*
